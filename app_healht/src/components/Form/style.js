import { StyleSheet } from "react-native ";

const styles = StyleSheet.create({
    formContent: {
        width: "100%",
        bottom:0,
        height:"100%",
        backgroundColor:"#fff",
        alignItems:"center",
        marginTop:30,
    },
});

export default styles